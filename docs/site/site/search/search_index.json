{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TraceGraph","text":"<p>A production-grade agent runtime for the JVM. Typed graphs, durable memory, deep observability.</p>"},{"location":"#why-tracegraph","title":"Why TraceGraph?","text":"<ul> <li>Typed graphs \u2014 compile-time safety through Java's type system</li> <li>Durable memory \u2014 cross-execution state with JDBC, file, and in-memory backends</li> <li>Deep observability \u2014 full trace replay, step-level diffs, and OpenTelemetry integration</li> <li>Spring Boot ready \u2014 auto-configuration for all SPIs</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>record MyState(String input, String output) {}\n\nGraph&lt;MyState&gt; graph = Graph.&lt;MyState&gt;builder()\n    .node(\"process\", (state, ctx) -&gt; new MyState(state.input(), \"Processed: \" + state.input()))\n    .entry(\"process\")\n    .build();\n\nvar result = graph.run(new MyState(\"hello\", null));\nSystem.out.println(result.finalState().output()); // Processed: hello\n</code></pre>"},{"location":"#installation","title":"Installation","text":"<pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;site.tracegraph&lt;/groupId&gt;\n    &lt;artifactId&gt;langgraph-core&lt;/artifactId&gt;\n    &lt;version&gt;0.2.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>"},{"location":"#modules","title":"Modules","text":"Module Purpose <code>langgraph-core</code> Graph engine, SPIs <code>langgraph-runtime</code> Checkpointing, retries, async <code>langgraph-observability</code> OTel, trace replay, diffs <code>langgraph-memory</code> Memory store implementations <code>langgraph-connectors</code> LLM clients, ReAct, tool-use <code>langgraph-rag</code> Vector stores, embeddings, retriever <code>langgraph-spring-boot-starter</code> Spring Boot auto-configuration"},{"location":"changelog/","title":"Changelog","text":"<p>See CHANGELOG.md in the repository for the full changelog.</p>"},{"location":"spring-boot/","title":"Spring Boot Integration","text":"<p>Add the starter to your <code>pom.xml</code>:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;site.tracegraph&lt;/groupId&gt;\n    &lt;artifactId&gt;langgraph-spring-boot-starter&lt;/artifactId&gt;\n    &lt;version&gt;0.2.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>The starter auto-configures no-op implementations of all SPIs. Override any with your own <code>@Bean</code>:</p> <pre><code>@Configuration\npublic class AppConfig {\n\n    @Bean\n    public Graph&lt;MyState&gt; graph(NodeListener listener, TraceRecorder recorder) {\n        return Graph.&lt;MyState&gt;builder()\n            .node(\"step1\", ...)\n            .entry(\"step1\")\n            .listener(listener)\n            .traceRecorder(recorder)\n            .build();\n    }\n\n    @Bean\n    public TraceStore traceStore() {\n        return new InMemoryTraceStore();\n    }\n}\n</code></pre>"},{"location":"spring-boot/#rest-endpoints","title":"REST Endpoints","text":"<p>With <code>TraceStore</code> in context, the starter exposes:</p> Endpoint Description <code>GET /tracegraph/traces</code> List all execution IDs <code>GET /tracegraph/traces/{id}</code> Full trace JSON <code>GET /tracegraph/traces/{a}/diff/{b}</code> Diff two traces <code>DELETE /tracegraph/traces/{id}</code> Delete a trace <code>POST /tracegraph/traces/{id}/replay?step=N</code> Re-run from step N <code>POST /tracegraph/traces/stream</code> SSE event stream"},{"location":"spring-boot/#configuration","title":"Configuration","text":"<pre><code>tracegraph.web.enabled=true\ntracegraph.memory.jdbc.enabled=false\ntracegraph.llm.provider=openai\ntracegraph.llm.api-key=sk-...\ntracegraph.llm.model=gpt-4o\n</code></pre>"},{"location":"concepts/connectors/","title":"Connectors","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"concepts/edges-routing/","title":"Edges Routing","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"concepts/graph-nodes/","title":"Graph Nodes","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"concepts/memory/","title":"Memory","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"concepts/observability/","title":"Observability","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"concepts/rag/","title":"Rag","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"concepts/state-context/","title":"State Context","text":"<p>Documentation coming soon. See the examples for working code.</p>"},{"location":"cookbook/hitl-approval/","title":"Hitl Approval","text":"<p>See the runnable example at <code>examples/hitl-approval/</code>.</p>"},{"location":"cookbook/rag-agent/","title":"Rag Agent","text":"<p>See the runnable example at <code>examples/rag-agent/</code>.</p>"},{"location":"cookbook/react-agent/","title":"React Agent","text":"<p>See the runnable example at <code>examples/react-agent/</code>.</p>"},{"location":"getting-started/first-graph/","title":"Your First Graph","text":"<p>This page walks you through building and running the simplest possible TraceGraph program \u2014 a two-node pipeline that transforms a string.</p>"},{"location":"getting-started/first-graph/#define-state","title":"Define state","text":"<p>State is any Java type you own. Records are the recommended choice because they are immutable by default.</p> <pre><code>record PipelineState(String input, String output) {}\n</code></pre>"},{"location":"getting-started/first-graph/#define-nodes","title":"Define nodes","text":"<p>A <code>Node&lt;S&gt;</code> is a functional interface: it receives the current state and a <code>Context</code>, and returns the next state.</p> <pre><code>Node&lt;PipelineState&gt; normalize = (state, ctx) -&gt;\n    new PipelineState(state.input().strip().toLowerCase(), null);\n\nNode&lt;PipelineState&gt; greet = (state, ctx) -&gt;\n    new PipelineState(state.input(), \"Hello, \" + state.input() + \"!\");\n</code></pre>"},{"location":"getting-started/first-graph/#build-the-graph","title":"Build the graph","text":"<pre><code>Graph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    .node(\"normalize\", normalize)\n    .node(\"greet\", greet)\n    .edge(\"normalize\", \"greet\")\n    .entry(\"normalize\")\n    .terminal(\"greet\")\n    .build();\n</code></pre> <p><code>entry</code> marks the first node that receives the seed state. <code>terminal</code> marks a node after which execution stops.</p>"},{"location":"getting-started/first-graph/#run-it","title":"Run it","text":"<pre><code>PipelineState initial = new PipelineState(\"  World  \", null);\nExecutionResult&lt;PipelineState&gt; result = graph.run(initial);\n\nSystem.out.println(result.finalState().output()); // Hello, world!\nSystem.out.println(result.status());              // COMPLETED\nSystem.out.println(result.executionId());         // UUID\n</code></pre> <p><code>Graph.run</code> is synchronous and blocks until the graph completes or fails. The returned <code>ExecutionResult</code> is an immutable record \u2014 safe to pass around freely.</p>"},{"location":"getting-started/first-graph/#key-takeaways","title":"Key takeaways","text":"<ul> <li>State is a plain Java type; records work best because they are immutable.</li> <li>Nodes are pure functions: <code>(S, Context) -&gt; S</code>. No side effects on the state object.</li> <li><code>entry</code> and <code>terminal</code> are required \u2014 the builder throws <code>GraphValidationException</code> without them.</li> <li><code>graph.run(seed)</code> returns an <code>ExecutionResult&lt;S&gt;</code> with <code>finalState</code>, <code>status</code>, and <code>executionId</code>.</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>TraceGraph requires JDK 21.</p>"},{"location":"getting-started/installation/#maven","title":"Maven","text":"<p>Add the modules you need:</p> <pre><code>&lt;!-- Core graph engine (always required) --&gt;\n&lt;dependency&gt;\n    &lt;groupId&gt;site.tracegraph&lt;/groupId&gt;\n    &lt;artifactId&gt;langgraph-core&lt;/artifactId&gt;\n    &lt;version&gt;0.2.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n\n&lt;!-- LLM connectors (OpenAI, Anthropic, Gemini, Ollama) --&gt;\n&lt;dependency&gt;\n    &lt;groupId&gt;site.tracegraph&lt;/groupId&gt;\n    &lt;artifactId&gt;langgraph-connectors&lt;/artifactId&gt;\n    &lt;version&gt;0.2.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n\n&lt;!-- RAG: vector stores, embeddings, retriever --&gt;\n&lt;dependency&gt;\n    &lt;groupId&gt;site.tracegraph&lt;/groupId&gt;\n    &lt;artifactId&gt;langgraph-rag&lt;/artifactId&gt;\n    &lt;version&gt;0.2.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n\n&lt;!-- Spring Boot starter (auto-config for all SPIs) --&gt;\n&lt;dependency&gt;\n    &lt;groupId&gt;site.tracegraph&lt;/groupId&gt;\n    &lt;artifactId&gt;langgraph-spring-boot-starter&lt;/artifactId&gt;\n    &lt;version&gt;0.2.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>"},{"location":"getting-started/installation/#snapshots","title":"Snapshots","text":"<p>SNAPSHOT artifacts are published to Maven Central Snapshots:</p> <pre><code>&lt;repository&gt;\n    &lt;id&gt;central-snapshots&lt;/id&gt;\n    &lt;url&gt;https://central.sonatype.com/repository/maven-snapshots/&lt;/url&gt;\n    &lt;snapshots&gt;&lt;enabled&gt;true&lt;/enabled&gt;&lt;/snapshots&gt;\n&lt;/repository&gt;\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>See the quickstart example for a runnable project.</p> <pre><code># Clone the repo\ngit clone https://github.com/kimho/TraceGraph\ncd TraceGraph\n\n# Run the quickstart example (no API keys needed)\nmvn -f examples/quickstart/pom.xml exec:java\n</code></pre> <p>Expected output: <pre><code>Final greeting: Hello, TraceGraph! (SHOUTED: HELLO, TRACEGRAPH!)\n</code></pre></p>"},{"location":"getting-started/spring-boot/","title":"Spring Boot Setup","text":"<p>TraceGraph ships a Spring Boot 3 starter that auto-wires SPI beans, exposes REST endpoints for traces, and optionally connects JDBC memory and LLM clients.</p>"},{"location":"getting-started/spring-boot/#add-the-dependency","title":"Add the dependency","text":"<pre><code>&lt;dependency&gt;\n  &lt;groupId&gt;io.tracegraph&lt;/groupId&gt;\n  &lt;artifactId&gt;tracegraph-spring-boot-starter&lt;/artifactId&gt;\n  &lt;version&gt;0.3.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>"},{"location":"getting-started/spring-boot/#declare-your-graph-as-a-bean","title":"Declare your graph as a bean","text":"<p>The starter does not register a <code>Graph&lt;?&gt;</code> \u2014 the state type <code>&lt;S&gt;</code> is yours to define.</p> <pre><code>@Configuration\npublic class AppConfig {\n\n    @Bean\n    Graph&lt;ChatState&gt; chatGraph(NodeListener listener, CheckpointStore checkpoints) {\n        return Graph.&lt;ChatState&gt;builder()\n            .node(\"ingest\", (s, ctx) -&gt; s.withInput(s.rawInput().strip()))\n            .node(\"respond\", (s, ctx) -&gt; s.withOutput(\"Echo: \" + s.input()))\n            .edge(\"ingest\", \"respond\")\n            .entry(\"ingest\")\n            .terminal(\"respond\")\n            .listener(listener)\n            .checkpointStore(checkpoints)\n            .build();\n    }\n}\n</code></pre> <p>The <code>NodeListener</code> and <code>CheckpointStore</code> beans are registered by <code>TraceGraphAutoConfiguration</code> with no-op defaults; your own <code>@Bean</code> definitions override them automatically via <code>@ConditionalOnMissingBean</code>.</p>"},{"location":"getting-started/spring-boot/#enable-the-trace-rest-api","title":"Enable the trace REST API","text":"<p>Add <code>tracegraph-observability</code> to your classpath and declare a <code>TraceStore</code> bean. The starter then registers <code>TraceController</code> automatically:</p> <pre><code>&lt;dependency&gt;\n  &lt;groupId&gt;io.tracegraph&lt;/groupId&gt;\n  &lt;artifactId&gt;tracegraph-observability&lt;/artifactId&gt;\n  &lt;version&gt;0.3.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <pre><code>@Bean\nTraceStore&lt;ChatState&gt; traceStore() {\n    return new InMemoryTraceStore&lt;&gt;();\n}\n</code></pre> <p>Once the bean is present, <code>GET /tracegraph/traces</code> is live.</p>"},{"location":"getting-started/spring-boot/#configure-an-llm-provider","title":"Configure an LLM provider","text":"<pre><code>tracegraph:\n  llm:\n    provider: openai\n    openai:\n      api-key: ${OPENAI_API_KEY}\n      model: gpt-4o-mini\n</code></pre> <p>This registers an <code>OpenAiLlmClient</code> bean that your nodes can inject.</p>"},{"location":"getting-started/spring-boot/#key-takeaways","title":"Key takeaways","text":"<ul> <li>Declare <code>Graph&lt;YourState&gt;</code> as a <code>@Bean</code> \u2014 the starter wires the SPIs around it.</li> <li>No-op defaults for all four SPIs (<code>NodeListener</code>, <code>CheckpointStore</code>, <code>TraceRecorder</code>, <code>MemoryStore</code>) are overridden by your own beans.</li> <li>The trace REST endpoints appear only when a <code>TraceStore</code> bean is present on the classpath.</li> <li>LLM auto-config activates via <code>tracegraph.llm.provider</code>.</li> </ul>"},{"location":"reference/complexity/","title":"Graph Complexity","text":"<p><code>GraphComplexity</code> is an immutable record returned by <code>graph.complexity()</code> and the <code>GET /tracegraph/ui/complexity</code> endpoint. It surfaces structural metrics about a compiled graph so you can reason about maintainability, testability, and runtime behaviour before deploying.</p>"},{"location":"reference/complexity/#fields","title":"Fields","text":"Field Type Description <code>nodeCount</code> int Total number of nodes in the graph, including parallel branches and subgraph nodes counted at the parent level. <code>edgeCount</code> int Total number of directed edges, including conditional edges. <code>maxFanOut</code> int Highest number of outgoing edges from any single node. A fan-out of 1 means linear flow; higher values indicate branching decision points. <code>maxDepth</code> int Length of the longest path from the entry node to any terminal node, measured in node hops. <code>cyclomaticComplexity</code> int Classic McCabe complexity: <code>edgeCount - nodeCount + 2</code>. Correlates with the minimum number of test cases needed for full path coverage. <code>parallelBranches</code> int Total number of anonymous parallel branches across all <code>parallel(...)</code> and <code>sendAll</code> call sites. <code>subgraphDepth</code> int Maximum nesting depth of embedded subgraphs. A flat graph has depth 0; one level of <code>subgraph(...)</code> gives depth 1. <code>hotspots</code> <code>List&lt;String&gt;</code> Node names that exceed a heuristic threshold for complexity contribution \u2014 typically nodes with high fan-out or that appear on many longest paths. Review these nodes first when debugging or profiling."},{"location":"reference/complexity/#when-to-act","title":"When to act","text":"Metric Warning threshold Action <code>nodeCount</code> &gt; 30 Consider breaking the graph into subgraphs. <code>cyclomaticComplexity</code> &gt; 10 Add tests for uncovered paths; consider simplifying routing logic. <code>maxFanOut</code> &gt; 5 A node is making too many routing decisions. Consider a dedicated <code>RoutingNode</code> or splitting into sub-decisions. <code>maxDepth</code> &gt; 15 Long chains are hard to debug. Checkpoint intermediate milestones and consider subgraph decomposition. <code>subgraphDepth</code> &gt; 3 Deep nesting makes trace navigation difficult. Flatten where possible. <code>parallelBranches</code> &gt; 10 High parallelism strains the executor. Verify the configured thread pool can handle the load."},{"location":"reference/complexity/#example-output","title":"Example output","text":"<pre><code>{\n  \"nodeCount\": 8,\n  \"edgeCount\": 10,\n  \"maxFanOut\": 3,\n  \"maxDepth\": 6,\n  \"cyclomaticComplexity\": 4,\n  \"parallelBranches\": 2,\n  \"subgraphDepth\": 1,\n  \"hotspots\": [\"route\", \"enrich\"]\n}\n</code></pre> <p>A <code>cyclomaticComplexity</code> of 4 means at least 4 distinct execution paths through the graph. <code>hotspots: [\"route\", \"enrich\"]</code> tells you to start your review with those two nodes.</p>"},{"location":"reference/complexity/#accessing-complexity","title":"Accessing complexity","text":"<p>Programmatically:</p> <pre><code>GraphComplexity c = graph.complexity();\nif (c.cyclomaticComplexity() &gt; 10) {\n    log.warn(\"Graph complexity {} exceeds threshold\", c.cyclomaticComplexity());\n}\n</code></pre> <p>Via REST (requires <code>tracegraph-ui</code>):</p> <pre><code>GET /tracegraph/ui/complexity\n</code></pre> <p>Returns the same JSON representation.</p>"},{"location":"reference/configuration/","title":"Configuration Reference","text":"<p>All TraceGraph properties use the <code>tracegraph.*</code> prefix. Set them in <code>application.yml</code> or <code>application.properties</code>.</p>"},{"location":"reference/configuration/#web-rest-api","title":"Web / REST API","text":"Property Type Default Description <code>tracegraph.web.enabled</code> boolean <code>true</code> When <code>false</code>, suppresses the entire <code>TraceWebAutoConfiguration</code> \u2014 no <code>TraceController</code>, <code>TraceReplayController</code>, or <code>TraceStreamController</code> beans are registered. <p>Example \u2014 disable the REST API entirely:</p> <pre><code>tracegraph:\n  web:\n    enabled: false\n</code></pre>"},{"location":"reference/configuration/#trace-ui","title":"Trace UI","text":"Property Type Default Description <code>tracegraph.ui.enabled</code> boolean <code>true</code> When <code>false</code>, suppresses the UI auto-config. The <code>/tracegraph/ui/*</code> endpoints and static assets are not served."},{"location":"reference/configuration/#memory-jdbc","title":"Memory (JDBC)","text":"<p>These properties control <code>MemoryAutoConfiguration</code>, which registers a <code>JdbcMemoryStore</code> when <code>tracegraph-memory</code>, <code>jackson-databind</code>, and a <code>DataSource</code> bean are all present.</p> Property Type Default Description <code>tracegraph.memory.jdbc.enabled</code> boolean <code>true</code> Set to <code>false</code> to skip <code>JdbcMemoryStore</code> auto-registration. The no-op <code>MemoryStore</code> from <code>TraceGraphAutoConfiguration</code> is used instead (unless you declare your own bean). <code>tracegraph.memory.jdbc.init-schema</code> boolean <code>true</code> When <code>true</code>, calls <code>initSchema()</code> on startup to create the <code>tracegraph_memory</code> table if it does not exist. Set to <code>false</code> if you manage schema migrations yourself. <code>tracegraph.memory.jdbc.table</code> string <code>tracegraph_memory</code> Override the table name. Useful when the default conflicts with an existing schema. <p>Example:</p> <pre><code>tracegraph:\n  memory:\n    jdbc:\n      enabled: true\n      init-schema: false      # managed by Flyway\n      table: tg_memory\n</code></pre>"},{"location":"reference/configuration/#llm-providers","title":"LLM Providers","text":"<p><code>LlmAutoConfiguration</code> registers an <code>LlmClient</code> bean when <code>tracegraph-connectors</code> is on the classpath and <code>tracegraph.llm.provider</code> is set. It is guarded by <code>@ConditionalOnMissingBean(LlmClient)</code> \u2014 declare your own bean to override.</p> Property Type Default Description <code>tracegraph.llm.enabled</code> boolean <code>true</code> Set to <code>false</code> to disable <code>LlmAutoConfiguration</code> entirely \u2014 no <code>LlmClient</code> bean is registered. <code>tracegraph.llm.provider</code> string (none) Required to activate an LLM bean. Accepted values: <code>openai</code>, <code>anthropic</code>. No bean is registered when unset."},{"location":"reference/configuration/#openai","title":"OpenAI","text":"Property Type Default Description <code>tracegraph.llm.openai.api-key</code> string \u2014 OpenAI API key. Required when <code>provider=openai</code>. <code>tracegraph.llm.openai.model</code> string <code>gpt-4o-mini</code> Model ID passed in every request. <code>tracegraph.llm.openai.base-url</code> string <code>https://api.openai.com/v1</code> Override for OpenAI-compatible endpoints (Azure, local proxies). <code>tracegraph.llm.openai.timeout-seconds</code> int <code>30</code> Request timeout for the underlying JDK <code>HttpClient</code>."},{"location":"reference/configuration/#anthropic","title":"Anthropic","text":"Property Type Default Description <code>tracegraph.llm.anthropic.api-key</code> string \u2014 Anthropic API key. Required when <code>provider=anthropic</code>. <code>tracegraph.llm.anthropic.model</code> string <code>claude-3-5-haiku-20241022</code> Model ID. <code>tracegraph.llm.anthropic.anthropic-version</code> string <code>2023-06-01</code> Value of the <code>anthropic-version</code> request header. <code>tracegraph.llm.anthropic.timeout-seconds</code> int <code>60</code> Request timeout. LLM calls can be slow \u2014 set conservatively. <p>Example \u2014 OpenAI:</p> <pre><code>tracegraph:\n  llm:\n    provider: openai\n    openai:\n      api-key: ${OPENAI_API_KEY}\n      model: gpt-4o\n      timeout-seconds: 45\n</code></pre> <p>Example \u2014 Anthropic:</p> <pre><code>tracegraph:\n  llm:\n    provider: anthropic\n    anthropic:\n      api-key: ${ANTHROPIC_API_KEY}\n      model: claude-3-5-sonnet-20241022\n</code></pre>"},{"location":"reference/configuration/#auto-configuration-order","title":"Auto-configuration order","text":"<p><code>MemoryAutoConfiguration</code> runs before <code>TraceGraphAutoConfiguration</code> so that a <code>JdbcMemoryStore</code> wins over the no-op default. <code>LlmAutoConfiguration</code> is independent.</p> <p><code>JdbcCheckpointStore</code> and <code>JdbcTraceStore</code> are not auto-wired because they require a user-supplied <code>Class&lt;S&gt;</code> \u2014 declare them as explicit <code>@Bean</code> definitions.</p>"},{"location":"reference/rest-api/","title":"REST API Reference","text":"<p>The TraceGraph Spring Boot starter exposes a set of HTTP endpoints for managing traces, triggering replays, resuming interrupted runs, and streaming execution events. All endpoints are under the <code>/tracegraph</code> path prefix.</p> <p>Endpoints are registered only when the relevant conditions are met (see Configuration for toggle properties).</p>"},{"location":"reference/rest-api/#traces","title":"Traces","text":""},{"location":"reference/rest-api/#list-traces","title":"List traces","text":"<pre><code>GET /tracegraph/traces\n</code></pre> <p>Returns a JSON array of execution IDs, ordered by <code>started_at</code> ascending.</p> <p>Query parameters</p> Parameter Type Default Description <code>limit</code> int (none) Maximum number of IDs to return. <code>offset</code> int 0 Number of IDs to skip before returning. <p>Response headers</p> Header Description <code>X-Total-Count</code> Unpaginated total number of stored traces. <p>Response \u2014 200</p> <pre><code>[\"a1b2c3d4-...\", \"e5f6g7h8-...\"]\n</code></pre> <p>Error responses</p> Status Condition 400 <code>limit</code> or <code>offset</code> is negative."},{"location":"reference/rest-api/#get-a-trace","title":"Get a trace","text":"<pre><code>GET /tracegraph/traces/{id}\n</code></pre> <p>Returns the full <code>ExecutionTrace</code> JSON for the given execution ID.</p> <p>Path parameters</p> Parameter Description <code>id</code> Execution ID (UUID string). <p>Response \u2014 200</p> <pre><code>{\n  \"executionId\": \"a1b2c3d4-...\",\n  \"status\": \"COMPLETED\",\n  \"startedAt\": \"2025-05-06T10:00:00Z\",\n  \"completedAt\": \"2025-05-06T10:00:01Z\",\n  \"steps\": [\n    {\n      \"nodeName\": \"fetch\",\n      \"before\": { \"input\": \"hello\" },\n      \"after\":  { \"input\": \"hello\", \"result\": \"world\" },\n      \"attempts\": 1,\n      \"usage\": { \"promptTokens\": 0, \"completionTokens\": 0 }\n    }\n  ]\n}\n</code></pre> <p>Error responses</p> Status Condition 404 No trace found for <code>id</code>."},{"location":"reference/rest-api/#delete-a-trace","title":"Delete a trace","text":"<pre><code>DELETE /tracegraph/traces/{id}\n</code></pre> <p>Removes the stored trace for the given execution ID.</p> <p>Response \u2014 204 (no body)</p> <p>Error responses</p> Status Condition 404 No trace found for <code>id</code>."},{"location":"reference/rest-api/#diff-two-traces","title":"Diff two traces","text":"<pre><code>GET /tracegraph/traces/{a}/diff/{b}\n</code></pre> <p>Computes a <code>TraceDiff</code> between trace <code>a</code> (left) and trace <code>b</code> (right).</p> <p>Path parameters</p> Parameter Description <code>a</code> Left execution ID. <code>b</code> Right execution ID. <p>Response \u2014 200</p> <pre><code>{\n  \"divergenceIndex\": 2,\n  \"sameStatus\": false,\n  \"sameFinalState\": false,\n  \"identical\": false,\n  \"leftRemainder\":  [ ... ],\n  \"rightRemainder\": [ ... ]\n}\n</code></pre> <p>Error responses</p> Status Condition 404 Either <code>a</code> or <code>b</code> is unknown."},{"location":"reference/rest-api/#replay","title":"Replay","text":""},{"location":"reference/rest-api/#replay-a-trace","title":"Replay a trace","text":"<pre><code>POST /tracegraph/traces/{id}/replay?step=N\n</code></pre> <p>Re-executes a saved trace from step index <code>N</code> using the current graph definition. Returns a new execution ID and fork lineage.</p> <p>Registered only when a single <code>Graph&lt;?&gt;</code> bean is present (<code>@ConditionalOnSingleCandidate</code>).</p> <p>Path parameters</p> Parameter Description <code>id</code> Source execution ID to replay. <p>Query parameters</p> Parameter Type Default Description <code>step</code> int -1 Step index to replay from. <code>-1</code> means replay from the entry node. <p>Response \u2014 200</p> <pre><code>{\n  \"executionId\": \"new-uuid-...\",\n  \"forkedFromExecutionId\": \"a1b2c3d4-...\",\n  \"forkedFromStepIndex\": 2,\n  \"status\": \"COMPLETED\"\n}\n</code></pre> <p>Error responses</p> Status Condition 404 No trace found for <code>id</code>. 400 <code>step</code> is out of range for the stored trace."},{"location":"reference/rest-api/#interrupts","title":"Interrupts","text":""},{"location":"reference/rest-api/#resume-an-interrupted-run","title":"Resume an interrupted run","text":"<pre><code>POST /tracegraph/traces/{id}/resume\n</code></pre> <p>Continues a run that is in <code>INTERRUPTED</code> status from its checkpoint.</p> <p>Registered on <code>TraceReplayController</code> (same conditions as replay endpoint).</p> <p>Path parameters</p> Parameter Description <code>id</code> Execution ID of an interrupted run. <p>Response \u2014 200</p> <pre><code>{\n  \"executionId\": \"a1b2c3d4-...\",\n  \"status\": \"COMPLETED\"\n}\n</code></pre> <p>Error responses</p> Status Condition 404 No trace found for <code>id</code>. 409 Run exists but is not in <code>INTERRUPTED</code> status."},{"location":"reference/rest-api/#streaming","title":"Streaming","text":""},{"location":"reference/rest-api/#stream-execution-events-sse","title":"Stream execution events (SSE)","text":"<pre><code>POST /tracegraph/traces/stream\n</code></pre> <p>Starts a new graph execution and streams <code>NodeEvent&lt;S&gt;</code> objects as Server-Sent Events. Event types: <code>NodeEnter</code>, <code>NodeExit</code>, <code>NodeRetry</code>, <code>Failed</code>, <code>Complete</code>.</p> <p>Registered only when a single <code>Graph&lt;?&gt;</code> bean is present.</p> <p>Request body \u2014 the seed state object as JSON.</p> <p>Response \u2014 <code>text/event-stream</code></p> <pre><code>event: NodeEnter\ndata: {\"nodeName\":\"fetch\",\"executionId\":\"...\"}\n\nevent: NodeExit\ndata: {\"nodeName\":\"fetch\",\"state\":{...},\"executionId\":\"...\"}\n\nevent: Complete\ndata: {\"finalState\":{...},\"executionId\":\"...\",\"status\":\"COMPLETED\"}\n</code></pre> <p>Backpressure overflow drops the oldest event; durable records live in the <code>TraceStore</code>.</p>"},{"location":"reference/rest-api/#trace-ui","title":"Trace UI","text":"<p>The following endpoints are registered by the <code>tracegraph-ui</code> module when present on the classpath.</p>"},{"location":"reference/rest-api/#graph-structure","title":"Graph structure","text":"<pre><code>GET /tracegraph/ui/graph\n</code></pre> <p>Returns the graph's structural description (nodes, edges, entry, terminals, subgraph nesting) as JSON. Used by the Trace UI graph view.</p>"},{"location":"reference/rest-api/#graph-complexity","title":"Graph complexity","text":"<pre><code>GET /tracegraph/ui/complexity\n</code></pre> <p>Returns a <code>GraphComplexity</code> record as JSON. See Graph Complexity for field definitions.</p>"},{"location":"trace-ui/overview/","title":"Trace UI \u2014 Overview","text":"<p>The Trace UI is a browser-based dashboard for inspecting, replaying, and comparing agent executions. It reads directly from the TraceGraph REST API so there is no separate backend to deploy.</p>"},{"location":"trace-ui/overview/#prerequisites","title":"Prerequisites","text":"<p>To use the Trace UI you need:</p> <ol> <li><code>tracegraph-spring-boot-starter</code> on the classpath.</li> <li>A <code>Graph&lt;?&gt;</code> bean declared in your Spring application context.</li> <li>A <code>TraceStore</code> bean \u2014 <code>InMemoryTraceStore</code> works for development; <code>JsonFileTraceStore</code> or <code>JdbcTraceStore</code> for production.</li> <li><code>tracegraph-observability</code> on the classpath (provides <code>TraceStore</code>, <code>RecordingTraceRecorder</code>, and related types).</li> </ol>"},{"location":"trace-ui/overview/#enabling-the-ui","title":"Enabling the UI","text":"<p>The UI is enabled by default when the above conditions are met. The static assets and API routes are served by <code>TraceUiAutoConfiguration</code>.</p> <p>To disable:</p> <pre><code>tracegraph:\n  ui:\n    enabled: false\n</code></pre>"},{"location":"trace-ui/overview/#accessing-the-ui","title":"Accessing the UI","text":"<p>Once your Spring Boot application is running, open:</p> <pre><code>http://localhost:8080/tracegraph/ui/\n</code></pre> <p>The dashboard loads in the browser and connects to the REST API at the same origin. No CORS configuration is needed for same-origin use.</p>"},{"location":"trace-ui/overview/#the-four-views","title":"The four views","text":"View URL fragment Purpose Trace list <code>#/traces</code> Browse all stored executions with status and timestamp. Trace detail <code>#/traces/:id</code> Step-by-step state diff for one execution; trigger replay from any step. Graph structure <code>#/graph</code> Interactive DAG render of the compiled graph (nodes, edges, subgraph clusters). Diff view <code>#/diff</code> Side-by-side comparison of two execution traces. <p>See Views Guide for full details on each view.</p>"},{"location":"trace-ui/overview/#architecture-note","title":"Architecture note","text":"<p>The UI is a single-page application served as static assets from the <code>tracegraph-ui</code> JAR. It calls the standard TraceGraph REST endpoints \u2014 the same ones documented in the REST API reference. You can use the REST API directly (e.g., from <code>curl</code> or Postman) without the UI.</p>"},{"location":"trace-ui/views/","title":"Trace UI \u2014 Views Guide","text":"<p>The Trace UI has four views accessible via the top navigation bar. This page describes what each view shows and how to use it.</p>"},{"location":"trace-ui/views/#trace-list-traces","title":"Trace List (<code>#/traces</code>)","text":"<p>The trace list is the home screen. It shows every stored execution as a table row with:</p> <ul> <li>Execution ID \u2014 click to open the detail view.</li> <li>Status \u2014 <code>COMPLETED</code>, <code>FAILED</code>, <code>INTERRUPTED</code>, or <code>RUNNING</code>.</li> <li>Started at / Completed at \u2014 ISO timestamps.</li> <li>Node count \u2014 number of steps recorded.</li> </ul> <p>Pagination \u2014 use the Previous / Next buttons or set the page size to control how many rows are loaded at a time. The <code>X-Total-Count</code> response header drives the page count display.</p> <p>Filtering \u2014 type a partial execution ID or status into the search box to narrow results client-side.</p>"},{"location":"trace-ui/views/#trace-detail-tracesid","title":"Trace Detail (<code>#/traces/:id</code>)","text":"<p>The detail view shows the full step-by-step execution history for one trace.</p>"},{"location":"trace-ui/views/#step-list","title":"Step list","text":"<p>Each row is a <code>TraceStep</code> with:</p> <ul> <li>Node name \u2014 the node that produced this step.</li> <li>Attempts \u2014 how many times the node was retried before succeeding (or failing).</li> <li>Token usage \u2014 prompt and completion tokens for LLM nodes.</li> <li>Status icon \u2014 green tick for success, red X for failure.</li> </ul> <p>Click a row to expand the state diff panel.</p>"},{"location":"trace-ui/views/#state-diff-panel","title":"State diff panel","text":"<p>Shows the before/after state for the selected step as a two-column JSON diff. Changed fields are highlighted. This is the primary tool for understanding what a node did to the state.</p>"},{"location":"trace-ui/views/#replay-from-step","title":"Replay from step","text":"<p>Each step row has a Replay from here button. Clicking it sends:</p> <pre><code>POST /tracegraph/traces/{id}/replay?step=N\n</code></pre> <p>The new execution ID appears in a toast notification and the trace list refreshes. The forked trace shows <code>forkedFrom</code> metadata in its detail view.</p>"},{"location":"trace-ui/views/#resume-interrupted-runs","title":"Resume (interrupted runs)","text":"<p>When the trace status is <code>INTERRUPTED</code>, a Resume button appears at the top of the detail view. Clicking it sends:</p> <pre><code>POST /tracegraph/traces/{id}/resume\n</code></pre> <p>The view polls for the updated status until the run completes or fails.</p>"},{"location":"trace-ui/views/#graph-structure-graph","title":"Graph Structure (<code>#/graph</code>)","text":"<p>The graph view renders the compiled graph as an interactive directed acyclic graph (DAG).</p> <ul> <li>Nodes are rectangles labelled with their name. Entry nodes are marked with an arrow-in icon; terminals with a double border.</li> <li>Edges are directed arrows. Conditional edges show a predicate badge.</li> <li>Subgraphs are rendered as labelled clusters (collapsible).</li> <li>Parallel groups are shown as a dashed box containing the anonymous branches.</li> </ul> <p>Hover over a node to see its retry policy (if any). Click a node name to jump to the most recent trace step for that node in the last execution.</p> <p>The graph data comes from <code>GET /tracegraph/ui/graph</code>.</p>"},{"location":"trace-ui/views/#diff-view-diff","title":"Diff View (<code>#/diff</code>)","text":"<p>The diff view compares two execution traces side by side.</p> <ol> <li>Select the left trace and right trace from the dropdowns (or paste execution IDs).</li> <li>Click Compare.</li> </ol> <p>The view calls <code>GET /tracegraph/traces/{a}/diff/{b}</code> and renders:</p> <ul> <li>Matched prefix \u2014 steps that are identical in both traces (node name + before/after state).</li> <li>Divergence point \u2014 the step index where the two traces first differ, highlighted in orange.</li> <li>Left remainder \u2014 steps that only exist in the left trace after divergence.</li> <li>Right remainder \u2014 steps that only exist in the right trace after divergence.</li> <li>Summary badges \u2014 same status? same final state? identical?</li> </ul> <p>Use the diff view to compare a baseline execution against a replay to understand the impact of a prompt change or graph modification.</p>"},{"location":"tutorial/01-nodes-and-edges/","title":"01 \u2014 Nodes &amp; Edges","text":"<p>Nodes and edges are the two primitives that every TraceGraph program is built from. This tutorial introduces both and shows how they combine into a runnable graph.</p>"},{"location":"tutorial/01-nodes-and-edges/#state-record","title":"State record","text":"<p>All tutorials share a common state record that evolves as features are added.</p> <pre><code>record PipelineState(String input, String cleaned, String result) {\n    static PipelineState of(String input) {\n        return new PipelineState(input, null, null);\n    }\n}\n</code></pre>"},{"location":"tutorial/01-nodes-and-edges/#nodes","title":"Nodes","text":"<p>A <code>Node&lt;S&gt;</code> is a <code>@FunctionalInterface</code> with signature <code>(S state, Context ctx) -&gt; S</code>. The node receives the current state, performs its work, and returns the next state. It must never mutate the state object it receives.</p> <pre><code>Node&lt;PipelineState&gt; clean = (state, ctx) -&gt;\n    new PipelineState(state.input(), state.input().strip().toLowerCase(), null);\n\nNode&lt;PipelineState&gt; shout = (state, ctx) -&gt;\n    new PipelineState(state.input(), state.cleaned(), state.cleaned().toUpperCase() + \"!\");\n</code></pre>"},{"location":"tutorial/01-nodes-and-edges/#unconditional-edges","title":"Unconditional edges","text":"<p><code>.edge(from, to)</code> creates a directed edge that is always followed after the source node completes.</p> <pre><code>Graph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    .node(\"clean\", clean)\n    .node(\"shout\", shout)\n    .edge(\"clean\", \"shout\")\n    .entry(\"clean\")\n    .terminal(\"shout\")\n    .build();\n</code></pre>"},{"location":"tutorial/01-nodes-and-edges/#conditional-edges","title":"Conditional edges","text":"<p><code>.edge(from, to, predicate)</code> adds a guarded edge. The first edge whose predicate returns <code>true</code> (in declaration order) is followed.</p> <pre><code>Node&lt;PipelineState&gt; warn = (state, ctx) -&gt;\n    new PipelineState(state.input(), state.cleaned(), \"[WARNING] empty input\");\n\nGraph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    .node(\"clean\", clean)\n    .node(\"shout\", shout)\n    .node(\"warn\", warn)\n    .edge(\"clean\", \"warn\",  s -&gt; s.cleaned().isEmpty())\n    .edge(\"clean\", \"shout\", s -&gt; !s.cleaned().isEmpty())\n    .entry(\"clean\")\n    .terminal(\"shout\")\n    .terminal(\"warn\")\n    .build();\n</code></pre> <p>Edge predicates must be pure functions of state \u2014 they are re-evaluated on resume after a checkpoint, so side effects inside a predicate will cause inconsistent routing.</p>"},{"location":"tutorial/01-nodes-and-edges/#running","title":"Running","text":"<pre><code>ExecutionResult&lt;PipelineState&gt; result = graph.run(PipelineState.of(\"  Hello  \"));\nSystem.out.println(result.finalState().result()); // HELLO!\n</code></pre>"},{"location":"tutorial/01-nodes-and-edges/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>Node&lt;S&gt;</code> is <code>(S, Context) -&gt; S</code> \u2014 return the new state, never mutate the old one.</li> <li>Unconditional edges with <code>.edge(from, to)</code> always fire.</li> <li>Conditional edges with <code>.edge(from, to, predicate)</code> are evaluated in declaration order; first match wins.</li> <li>Edge predicates must be pure \u2014 no side effects, no I/O.</li> </ul>"},{"location":"tutorial/02-state-and-context/","title":"02 \u2014 State &amp; Context","text":"<p>Every node receives two arguments: the current state and a <code>Context</code> object. This tutorial explains what <code>Context</code> provides and how to design state for real workloads.</p>"},{"location":"tutorial/02-state-and-context/#evolving-the-state-record","title":"Evolving the state record","text":"<p>Real pipelines accumulate data as they progress. Add fields for the execution metadata you need:</p> <pre><code>record PipelineState(\n    String input,\n    String cleaned,\n    String result,\n    List&lt;String&gt; log\n) {\n    static PipelineState of(String input) {\n        return new PipelineState(input, null, null, List.of());\n    }\n\n    PipelineState withLog(String entry) {\n        var next = new ArrayList&lt;&gt;(log);\n        next.add(entry);\n        return new PipelineState(input, cleaned, result, List.copyOf(next));\n    }\n}\n</code></pre>"},{"location":"tutorial/02-state-and-context/#using-context","title":"Using Context","text":"<p><code>Context</code> is passed to every node and carries execution-scoped metadata.</p> <pre><code>Node&lt;PipelineState&gt; clean = (state, ctx) -&gt; {\n    String cleaned = state.input().strip().toLowerCase();\n    String logEntry = \"[%s] cleaned\".formatted(ctx.executionId());\n    return new PipelineState(state.input(), cleaned, null, state.log())\n        .withLog(logEntry);\n};\n</code></pre>"},{"location":"tutorial/02-state-and-context/#ctxexecutionid","title":"<code>ctx.executionId()</code>","text":"<p>A stable UUID for the current run. Use it when logging or correlating traces across services.</p>"},{"location":"tutorial/02-state-and-context/#ctxidempotencykey","title":"<code>ctx.idempotencyKey()</code>","text":"<p>A deterministic key derived from <code>executionId</code> + node name + attempt number. Pass it to upstream HTTP or JDBC calls to make retries safe without double-applying effects.</p> <pre><code>Node&lt;PipelineState&gt; callApi = (state, ctx) -&gt; {\n    String result = externalService.call(state.cleaned(), ctx.idempotencyKey());\n    return new PipelineState(state.input(), state.cleaned(), result, state.log());\n};\n</code></pre>"},{"location":"tutorial/02-state-and-context/#ctxmemory","title":"<code>ctx.memory()</code>","text":"<p>Access to the <code>MemoryStore</code> for cross-execution persistence. Covered in depth in Tutorial 06.</p>"},{"location":"tutorial/02-state-and-context/#ctxreportusageprompttokens-completiontokens","title":"<code>ctx.reportUsage(promptTokens, completionTokens)</code>","text":"<p>Used by LLM nodes to report token consumption to listeners. Covered in Tutorial 07.</p>"},{"location":"tutorial/02-state-and-context/#state-composition-vs-generic-result-types","title":"State composition vs. generic result types","text":"<p>TraceGraph uses a single type parameter <code>&lt;S&gt;</code>. Instead of <code>Node&lt;S, R&gt;</code>, fold sub-results back into the state:</p> <pre><code>// Good \u2014 sub-result is a field on the state record\nrecord PipelineState(String input, String cleaned, String result, ...) {}\n\n// Avoid \u2014 two type parameters break builder inference and complicate resumption\n// Node&lt;S, R&gt;  \u2190 not how TraceGraph works\n</code></pre>"},{"location":"tutorial/02-state-and-context/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>ctx.executionId()</code> gives a stable UUID per run; use it for logging and correlation.</li> <li><code>ctx.idempotencyKey()</code> is attempt-scoped; pass it to external calls to enable safe retries.</li> <li>State is a value type \u2014 nodes return a new state, never mutate the received one.</li> <li>Fold sub-results into fields on the state record rather than adding type parameters.</li> </ul>"},{"location":"tutorial/03-retries-and-failures/","title":"03 \u2014 Retries &amp; Failures","text":"<p>Production nodes call external services that fail transiently. TraceGraph's retry system is part of graph definition \u2014 not runtime configuration \u2014 so the policy is reproducible, versionable, and visible in traces.</p>"},{"location":"tutorial/03-retries-and-failures/#retrypolicy","title":"RetryPolicy","text":"<p>Create a policy with <code>RetryPolicy.of(maxAttempts, backoffStrategy)</code>:</p> <pre><code>RetryPolicy fixed      = RetryPolicy.of(3, BackoffStrategy.fixed(200));\nRetryPolicy exponential = RetryPolicy.of(5, BackoffStrategy.exponential(100, 10_000));\n</code></pre> <p><code>BackoffStrategy.fixed(ms)</code> waits a constant number of milliseconds between attempts. <code>BackoffStrategy.exponential(baseMs, maxMs)</code> doubles the delay each attempt, capped at <code>maxMs</code>.</p>"},{"location":"tutorial/03-retries-and-failures/#attaching-a-policy-to-a-node","title":"Attaching a policy to a node","text":"<p>Pass the policy as a third argument to <code>.node(...)</code>:</p> <pre><code>RetryPolicy threeAttempts = RetryPolicy.of(3, BackoffStrategy.exponential(200, 5_000));\n\nGraph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    .node(\"callApi\", callApiNode, threeAttempts)\n    .node(\"process\", processNode)\n    .edge(\"callApi\", \"process\")\n    .entry(\"callApi\")\n    .terminal(\"process\")\n    .build();\n</code></pre>"},{"location":"tutorial/03-retries-and-failures/#default-retry-policy","title":"Default retry policy","text":"<p>Apply a policy to all nodes that don't have their own:</p> <pre><code>Graph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    .node(\"callApi\", callApiNode, RetryPolicy.of(5, BackoffStrategy.exponential(100, 8_000)))\n    .node(\"process\", processNode)\n    .defaultRetryPolicy(RetryPolicy.of(2, BackoffStrategy.fixed(500)))\n    .entry(\"callApi\")\n    .terminal(\"process\")\n    .build();\n</code></pre> <p>Per-node policy beats the default. <code>process</code> gets two attempts with 500 ms fixed backoff; <code>callApi</code> gets its own five-attempt exponential policy.</p>"},{"location":"tutorial/03-retries-and-failures/#idempotency-on-retry","title":"Idempotency on retry","text":"<p>Use <code>ctx.idempotencyKey()</code> to avoid double-applying effects. The key is stable across all attempts of the same node within the same execution:</p> <pre><code>Node&lt;PipelineState&gt; callApiNode = (state, ctx) -&gt; {\n    String response = httpClient.post(\n        \"/enrich\",\n        state.cleaned(),\n        Map.of(\"Idempotency-Key\", ctx.idempotencyKey())\n    );\n    return state.withResult(response);\n};\n</code></pre>"},{"location":"tutorial/03-retries-and-failures/#what-retries-do-not-cover","title":"What retries do not cover","text":"<ul> <li><code>Error</code> and <code>InterruptedException</code> always short-circuit the retry loop \u2014 they propagate immediately.</li> <li>A node that exhausts all attempts surfaces a <code>NodeExecutionException</code> which sets <code>ExecutionResult.status</code> to <code>FAILED</code>.</li> </ul> <pre><code>ExecutionResult&lt;PipelineState&gt; result = graph.run(PipelineState.of(\"hello\"));\nif (result.status() == Status.FAILED) {\n    result.failureCause().ifPresent(Throwable::printStackTrace);\n}\n</code></pre>"},{"location":"tutorial/03-retries-and-failures/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>RetryPolicy</code> is graph-definition time, not runtime config \u2014 it's reproducible across environments.</li> <li><code>BackoffStrategy.fixed</code> and <code>.exponential</code> cover the two common patterns.</li> <li>Per-node policy overrides <code>defaultRetryPolicy</code>.</li> <li>Use <code>ctx.idempotencyKey()</code> to make external calls idempotent across retry attempts.</li> <li><code>Error</code> and <code>InterruptedException</code> bypass retries entirely.</li> </ul>"},{"location":"tutorial/04-checkpoints-and-resume/","title":"04 \u2014 Checkpoints &amp; Resume","text":"<p>Long-running graphs that call LLMs or external APIs need the ability to survive process restarts. Checkpointing lets TraceGraph write progress after each node and resume from the last completed node on the next run.</p>"},{"location":"tutorial/04-checkpoints-and-resume/#how-checkpointing-works","title":"How checkpointing works","text":"<p>After a node exits successfully and before the outgoing edge is evaluated, TraceGraph writes a checkpoint recording the <code>executionId</code>, the <code>lastCompletedNode</code>, and the current state. On resume, it loads the checkpoint, re-evaluates the outgoing edges of <code>lastCompletedNode</code>, and continues from there.</p> <p>Because edge predicates are re-evaluated on resume, they must be pure functions of state \u2014 the same guarantee from Tutorial 01 becomes a correctness requirement here.</p>"},{"location":"tutorial/04-checkpoints-and-resume/#inmemorycheckpointstore-development","title":"InMemoryCheckpointStore (development)","text":"<pre><code>CheckpointStore&lt;PipelineState&gt; store = new InMemoryCheckpointStore&lt;&gt;();\n\nGraph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    .node(\"fetch\",   fetchNode,   RetryPolicy.of(3, BackoffStrategy.exponential(200, 5_000)))\n    .node(\"enrich\",  enrichNode)\n    .node(\"persist\", persistNode)\n    .edge(\"fetch\", \"enrich\")\n    .edge(\"enrich\", \"persist\")\n    .entry(\"fetch\")\n    .terminal(\"persist\")\n    .checkpointStore(store)\n    .build();\n</code></pre>"},{"location":"tutorial/04-checkpoints-and-resume/#jdbccheckpointstore-production","title":"JdbcCheckpointStore (production)","text":"<p><code>JdbcCheckpointStore</code> stores checkpoints in a single database table. Call <code>initSchema()</code> once at startup:</p> <pre><code>JdbcCheckpointStore&lt;PipelineState&gt; store =\n    new JdbcCheckpointStore&lt;&gt;(dataSource, PipelineState.class);\nstore.initSchema();\n\nGraph&lt;PipelineState&gt; graph = Graph.&lt;PipelineState&gt;builder()\n    // ... nodes and edges ...\n    .checkpointStore(store)\n    .build();\n</code></pre>"},{"location":"tutorial/04-checkpoints-and-resume/#running-and-resuming","title":"Running and resuming","text":"<pre><code>// First run \u2014 crashes mid-way through\nString executionId = UUID.randomUUID().toString();\nExecutionResult&lt;PipelineState&gt; result = graph.run(PipelineState.of(\"hello\"), executionId);\n\n// On restart \u2014 resume from where it left off\nExecutionResult&lt;PipelineState&gt; resumed = graph.resume(executionId);\nSystem.out.println(resumed.status()); // COMPLETED\n</code></pre> <p><code>graph.resume(id)</code> loads the checkpoint, skips completed nodes, and continues forward.</p>"},{"location":"tutorial/04-checkpoints-and-resume/#at-least-once-semantics","title":"At-least-once semantics","text":"<p>Nodes are at-least-once on resume. If a crash happens while a node is executing (after it started but before the checkpoint was written), that node re-runs from attempt 1 on resume. Design nodes to be idempotent using <code>ctx.idempotencyKey()</code>.</p>"},{"location":"tutorial/04-checkpoints-and-resume/#key-takeaways","title":"Key takeaways","text":"<ul> <li>Checkpoints are written after node exit, before edge resolution.</li> <li><code>InMemoryCheckpointStore</code> is suitable for tests and development; <code>JdbcCheckpointStore</code> for production.</li> <li>Nodes are at-least-once \u2014 use <code>ctx.idempotencyKey()</code> for external calls.</li> <li>Edge predicates must be pure because they are re-evaluated on resume.</li> </ul>"},{"location":"tutorial/05-parallel-and-send/","title":"05 \u2014 Parallel &amp; Send","text":"<p>TraceGraph supports two forms of concurrent execution: static <code>parallel</code> branches declared at graph-build time, and dynamic <code>sendAll</code> fan-out resolved at runtime inside a routing node.</p>"},{"location":"tutorial/05-parallel-and-send/#static-parallel-branches","title":"Static parallel branches","text":"<p><code>.parallel(name, branches, merger)</code> runs a fixed list of <code>Node&lt;S&gt;</code> branches concurrently. All branches receive the same input state; their results are merged in declaration order by the <code>merger</code> function.</p> <pre><code>record EnrichState(String input, String geoResult, String sentimentResult, String combined) {}\n\nNode&lt;EnrichState&gt; geoNode       = (s, ctx) -&gt; s.withGeoResult(geoApi.lookup(s.input()));\nNode&lt;EnrichState&gt; sentimentNode = (s, ctx) -&gt; s.withSentimentResult(sentimentApi.analyze(s.input()));\n\nGraph&lt;EnrichState&gt; graph = Graph.&lt;EnrichState&gt;builder()\n    .parallel(\n        \"enrich\",\n        List.of(geoNode, sentimentNode),\n        (a, b) -&gt; new EnrichState(a.input(), a.geoResult(), b.sentimentResult(), null)\n    )\n    .node(\"combine\", (s, ctx) -&gt; s.withCombined(s.geoResult() + \" | \" + s.sentimentResult()))\n    .edge(\"enrich\", \"combine\")\n    .entry(\"enrich\")\n    .terminal(\"combine\")\n    .build();\n</code></pre> <p>Branches run on a virtual-thread executor. They are anonymous \u2014 they don't appear in trace steps or fire listener events. If any branch throws, the first failure (in declaration order) propagates.</p>"},{"location":"tutorial/05-parallel-and-send/#custom-executor","title":"Custom executor","text":"<p>By default, a virtual-thread-per-task executor is created per <code>run</code> call and shut down on completion. Supply your own to control thread pool lifecycle:</p> <pre><code>ExecutorService sharedPool = Executors.newVirtualThreadPerTaskExecutor();\n\nGraph&lt;EnrichState&gt; graph = Graph.&lt;EnrichState&gt;builder()\n    // ... nodes ...\n    .executor(sharedPool)   // NOT shut down by the graph\n    .build();\n</code></pre> <p>User-supplied executors are never shut down by the graph.</p>"},{"location":"tutorial/05-parallel-and-send/#dynamic-fan-out-with-sendall","title":"Dynamic fan-out with sendAll","text":"<p>When the number of parallel targets is not known at build time, use <code>NodeResult.sendAll(...)</code> inside a <code>RoutingNode</code>:</p> <pre><code>record BatchState(List&lt;String&gt; items, Map&lt;String, String&gt; results) {}\n\nGraph&lt;BatchState&gt; graph = Graph.&lt;BatchState&gt;builder()\n    .routingNode(\"dispatch\", (state, ctx) -&gt; {\n        List&lt;Send&lt;BatchState&gt;&gt; sends = state.items().stream()\n            .map(item -&gt; new Send&lt;&gt;(\"process\", state.withSingleItem(item)))\n            .toList();\n        return NodeResult.sendAll(sends, BatchState::merge, state);\n    })\n    .node(\"process\", (s, ctx) -&gt; s.withResult(s.currentItem(), process(s.currentItem())))\n    .edge(\"process\", \"done\")\n    .entry(\"dispatch\")\n    .terminal(\"done\")\n    .build();\n</code></pre> <p><code>sendAll</code> expands identically to <code>parallel</code> at runtime \u2014 branches run concurrently on the configured executor.</p>"},{"location":"tutorial/05-parallel-and-send/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>parallel(name, branches, merger)</code> is declared at build time; all branches receive the same input state.</li> <li>Branches are anonymous \u2014 no names, no trace steps, no listener events.</li> <li>First-by-declaration-order failure wins when multiple branches throw.</li> <li><code>NodeResult.sendAll(...)</code> inside a routing node enables runtime-determined fan-out.</li> <li>User-supplied executors via <code>.executor(...)</code> are not shut down by the graph.</li> </ul>"},{"location":"tutorial/06-memory/","title":"06 \u2014 Memory","text":"<p>Working memory in TraceGraph is the state object itself \u2014 it lives only for the duration of one execution. The <code>MemoryStore</code> SPI provides cross-execution persistence: data that survives beyond a single run and is accessible by any node via <code>ctx.memory()</code>.</p>"},{"location":"tutorial/06-memory/#memorystore-spi","title":"MemoryStore SPI","text":"<p><code>MemoryStore</code> is a scoped key-value store:</p> <pre><code>ctx.memory().put(\"user:42\", \"preferences\", Map.of(\"lang\", \"en\"));\nObject prefs = ctx.memory().get(\"user:42\", \"preferences\");\nSet&lt;String&gt; keys = ctx.memory().keys(\"user:42\");\nctx.memory().delete(\"user:42\", \"preferences\");\n</code></pre> <p>The first argument is the scope (typically a user ID, session ID, or domain category) and the second is the key within that scope.</p>"},{"location":"tutorial/06-memory/#wiring-the-store","title":"Wiring the store","text":"<pre><code>MemoryStore store = new InMemoryMemoryStore();\n\nGraph&lt;ChatState&gt; graph = Graph.&lt;ChatState&gt;builder()\n    .node(\"recall\",  recallNode)\n    .node(\"respond\", respondNode)\n    .node(\"remember\", rememberNode)\n    .edge(\"recall\", \"respond\")\n    .edge(\"respond\", \"remember\")\n    .entry(\"recall\")\n    .terminal(\"remember\")\n    .memoryStore(store)\n    .build();\n</code></pre> <p>If no store is wired, <code>ctx.memory()</code> returns a no-op implementation that discards all writes silently.</p>"},{"location":"tutorial/06-memory/#reading-and-writing-in-nodes","title":"Reading and writing in nodes","text":"<pre><code>Node&lt;ChatState&gt; recallNode = (state, ctx) -&gt; {\n    @SuppressWarnings(\"unchecked\")\n    List&lt;String&gt; history = (List&lt;String&gt;) ctx.memory()\n        .get(\"session:\" + state.sessionId(), \"history\");\n    return state.withHistory(history != null ? history : List.of());\n};\n\nNode&lt;ChatState&gt; rememberNode = (state, ctx) -&gt; {\n    List&lt;String&gt; updated = new ArrayList&lt;&gt;(state.history());\n    updated.add(state.lastTurn());\n    ctx.memory().put(\"session:\" + state.sessionId(), \"history\", List.copyOf(updated));\n    return state;\n};\n</code></pre>"},{"location":"tutorial/06-memory/#filememorystore-and-jdbcmemorystore","title":"FileMemoryStore and JdbcMemoryStore","text":"<p>For production use, swap in a durable implementation:</p> <pre><code>// File-backed (useful for local development)\nMemoryStore fileStore = FileMemoryStore.of(Path.of(\"/var/tracegraph/memory\"));\n\n// JDBC (production)\nJdbcMemoryStore jdbcStore = new JdbcMemoryStore(dataSource);\njdbcStore.initSchema();\n</code></pre> <p>Both implementations support heterogeneous value types (strings, numbers, lists, maps) using Jackson polymorphic serialization. Path-traversal attacks are guarded \u2014 scope and key values containing <code>/</code>, <code>\\</code>, or <code>..</code> are rejected.</p>"},{"location":"tutorial/06-memory/#spring-boot-auto-wiring","title":"Spring Boot auto-wiring","text":"<p>When <code>tracegraph-memory</code>, <code>jackson-databind</code>, and a <code>DataSource</code> bean are all on the classpath, <code>MemoryAutoConfiguration</code> registers a <code>JdbcMemoryStore</code> automatically. Disable with <code>tracegraph.memory.jdbc.enabled=false</code>.</p>"},{"location":"tutorial/06-memory/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>ctx.memory()</code> is scoped key-value storage that persists across executions.</li> <li>The default (no store wired) is a no-op \u2014 writes are silently discarded.</li> <li><code>InMemoryMemoryStore</code> is suitable for tests; <code>FileMemoryStore</code> and <code>JdbcMemoryStore</code> for production.</li> <li>Scope + key pairs are validated against path-traversal attacks.</li> </ul>"},{"location":"tutorial/07-llm-and-tools/","title":"07 \u2014 LLM &amp; Tools","text":"<p>TraceGraph's connector module provides a vendor-neutral <code>LlmClient</code> interface and <code>ChatNode&lt;S&gt;</code> adapter that wires any LLM into a graph node.</p>"},{"location":"tutorial/07-llm-and-tools/#llmclient-interface","title":"LlmClient interface","text":"<pre><code>public interface LlmClient {\n    LlmResponse complete(LlmRequest request);\n    default Flow.Publisher&lt;LlmStreamChunk&gt; stream(LlmRequest request) { ... }\n}\n</code></pre> <p><code>LlmRequest</code> and <code>LlmResponse</code> are records covering the common denominator across chat APIs: messages, model, temperature, max tokens, usage, and finish reason.</p>"},{"location":"tutorial/07-llm-and-tools/#chatnode","title":"ChatNode","text":"<p><code>ChatNode&lt;S&gt;</code> bridges <code>LlmClient</code> and <code>Node&lt;S&gt;</code>. You supply two functions:</p> <ul> <li><code>requestBuilder</code>: <code>(S state) -&gt; LlmRequest</code> \u2014 build the request from the current state.</li> <li><code>responseFolder</code>: <code>(S state, LlmResponse response) -&gt; S</code> \u2014 fold the response back into the state.</li> </ul> <pre><code>record ChatState(String userMessage, String assistantReply, int promptTokens, int completionTokens) {}\n\nLlmClient client = OpenAiLlmClient.builder()\n    .apiKey(System.getenv(\"OPENAI_API_KEY\"))\n    .model(\"gpt-4o-mini\")\n    .build();\n\nNode&lt;ChatState&gt; chatNode = ChatNode.&lt;ChatState&gt;builder()\n    .client(client)\n    .requestBuilder(state -&gt; LlmRequest.builder()\n        .message(ChatMessage.user(state.userMessage()))\n        .model(\"gpt-4o-mini\")\n        .maxTokens(512)\n        .build())\n    .responseFolder((state, response) -&gt; new ChatState(\n        state.userMessage(),\n        response.content(),\n        response.usage().promptTokens(),\n        response.usage().completionTokens()\n    ))\n    .build();\n</code></pre> <p><code>ChatNode</code> automatically calls <code>ctx.reportUsage(promptTokens, completionTokens)</code> after each successful response, so token usage appears in trace steps and OTel spans.</p>"},{"location":"tutorial/07-llm-and-tools/#anthropicllmclient","title":"AnthropicLlmClient","text":"<pre><code>LlmClient anthropic = AnthropicLlmClient.builder()\n    .apiKey(System.getenv(\"ANTHROPIC_API_KEY\"))\n    .model(\"claude-3-5-haiku-20241022\")\n    .build();\n</code></pre> <p>System messages are lifted into the top-level <code>system</code> field required by the Anthropic Messages API. Non-2xx responses surface as <code>LlmHttpException(statusCode, body)</code>.</p>"},{"location":"tutorial/07-llm-and-tools/#streaming","title":"Streaming","text":"<pre><code>Flow.Publisher&lt;LlmStreamChunk&gt; chunks = client.stream(request);\nchunks.subscribe(new Flow.Subscriber&lt;&gt;() {\n    public void onNext(LlmStreamChunk chunk) {\n        System.out.print(chunk.delta());\n        if (chunk.isLast()) System.out.println();\n    }\n    // ... other lifecycle methods\n});\n</code></pre> <p>Providers without native streaming support fall back to a single-chunk publisher wrapping <code>complete()</code>.</p>"},{"location":"tutorial/07-llm-and-tools/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>LlmClient</code> is the vendor-neutral SPI; <code>OpenAiLlmClient</code> and <code>AnthropicLlmClient</code> are bundled adapters.</li> <li><code>ChatNode&lt;S&gt;</code> adapts any <code>LlmClient</code> into a <code>Node&lt;S&gt;</code> via <code>requestBuilder</code> and <code>responseFolder</code>.</li> <li>Token usage is reported automatically through <code>ctx.reportUsage(...)</code> and appears in traces and spans.</li> <li>Non-2xx responses surface as <code>LlmHttpException</code> \u2014 handle them in a retry policy or a failure edge.</li> </ul>"},{"location":"tutorial/08-react-agent/","title":"08 \u2014 ReAct Agent","text":"<p>The ReAct (Reason + Act) pattern alternates between asking the LLM to reason about what action to take and executing that action. <code>ReActAgent&lt;S&gt;</code> is a factory that builds a complete <code>Graph&lt;S&gt;</code> implementing this loop.</p>"},{"location":"tutorial/08-react-agent/#how-the-loop-works","title":"How the loop works","text":"<ol> <li>llm node \u2014 sends the current state (including tool results from prior steps) to the LLM.</li> <li>If the response contains tool calls \u2192 route to the tools node.</li> <li>tools node \u2014 executes each requested tool and appends results to state.</li> <li>Route back to llm node.</li> <li>When the LLM returns no tool calls \u2192 route to done (terminal).</li> </ol>"},{"location":"tutorial/08-react-agent/#define-state","title":"Define state","text":"<pre><code>record AgentState(\n    String userQuery,\n    List&lt;ChatMessage&gt; history,\n    List&lt;ToolResult&gt; lastToolResults,\n    String finalAnswer\n) {\n    static AgentState of(String query) {\n        return new AgentState(query, List.of(), List.of(), null);\n    }\n}\n</code></pre>"},{"location":"tutorial/08-react-agent/#define-tools","title":"Define tools","text":"<pre><code>Tool calculator = args -&gt; {\n    // args is a JSON string per the tool's parametersSchema\n    var input = Json.parse(args);\n    double result = eval(input.get(\"expression\").asText());\n    return String.valueOf(result);\n};\n\nToolDefinition calcDef = new ToolDefinition(\n    \"calculator\",\n    \"Evaluates a mathematical expression\",\n    \"\"\"\n    {\"type\":\"object\",\"properties\":{\"expression\":{\"type\":\"string\"}},\"required\":[\"expression\"]}\n    \"\"\"\n);\n</code></pre>"},{"location":"tutorial/08-react-agent/#build-the-agent-graph","title":"Build the agent graph","text":"<pre><code>LlmClient client = OpenAiLlmClient.builder()\n    .apiKey(System.getenv(\"OPENAI_API_KEY\"))\n    .model(\"gpt-4o-mini\")\n    .build();\n\nGraph&lt;AgentState&gt; agentGraph = ReActAgent.&lt;AgentState&gt;builder()\n    .client(client)\n    .tool(calcDef, calculator)\n    .requestFactory(state -&gt; LlmRequest.builder()\n        .messages(state.history())\n        .model(\"gpt-4o-mini\")\n        .build())\n    .responseFolder((state, response) -&gt; state.withHistory(\n        append(state.history(), ChatMessage.assistant(response.content()))\n    ))\n    .toolResultFolder((state, results) -&gt; state\n        .withLastToolResults(results)\n        .withHistory(append(state.history(), toMessages(results))))\n    .build()\n    .buildGraph();\n</code></pre>"},{"location":"tutorial/08-react-agent/#run-it","title":"Run it","text":"<pre><code>ExecutionResult&lt;AgentState&gt; result = agentGraph.run(AgentState.of(\"What is 42 * 17?\"));\nSystem.out.println(result.finalState().finalAnswer()); // 714.0\n</code></pre> <p>The agent will loop as many times as needed until the LLM stops requesting tools.</p>"},{"location":"tutorial/08-react-agent/#composing-the-agent-as-a-subgraph","title":"Composing the agent as a subgraph","text":"<p><code>ReActAgent</code> produces a regular <code>Graph&lt;S&gt;</code>. Use <code>.subgraph(name, agentGraph)</code> to embed it inside a larger graph:</p> <pre><code>Graph&lt;AppState&gt; pipeline = Graph.&lt;AppState&gt;builder()\n    .node(\"prepare\",   prepareNode)\n    .subgraph(\"agent\", agentGraph)   // inner graph shares &lt;AppState&gt;\n    .node(\"format\",    formatNode)\n    .edge(\"prepare\", \"agent\")\n    .edge(\"agent\",   \"format\")\n    .entry(\"prepare\")\n    .terminal(\"format\")\n    .build();\n</code></pre>"},{"location":"tutorial/08-react-agent/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>ReActAgent&lt;S&gt;.builder()</code> produces a complete <code>Graph&lt;S&gt;</code> \u2014 the llm/tools/done structure is wired for you.</li> <li>Supply <code>requestFactory</code> and <code>responseFolder</code> to control how state maps to <code>LlmRequest</code> and back.</li> <li><code>toolResultFolder</code> folds executed tool results back into the state before the next LLM call.</li> <li>The built graph is a regular <code>Graph&lt;S&gt;</code> \u2014 embed it as a subgraph or run it standalone.</li> </ul>"},{"location":"tutorial/09-rag-pipeline/","title":"09 \u2014 RAG Pipeline","text":"<p>Retrieval-Augmented Generation (RAG) combines a vector search step with an LLM generation step. In TraceGraph, each step is a node \u2014 giving you full observability, retries, and checkpointing over the entire retrieval-generation loop.</p>"},{"location":"tutorial/09-rag-pipeline/#state-record","title":"State record","text":"<pre><code>record RagState(\n    String query,\n    List&lt;String&gt; retrievedChunks,\n    String systemPrompt,\n    String answer\n) {\n    static RagState of(String query) {\n        return new RagState(query, List.of(), null, null);\n    }\n}\n</code></pre>"},{"location":"tutorial/09-rag-pipeline/#retrieve-node","title":"Retrieve node","text":"<p>The retrieve node calls a vector database and populates <code>retrievedChunks</code>. This example uses a hypothetical <code>VectorStore</code> abstraction \u2014 swap in any provider.</p> <pre><code>Node&lt;RagState&gt; retrieveNode = (state, ctx) -&gt; {\n    List&lt;String&gt; chunks = vectorStore.similaritySearch(state.query(), 5);\n    return new RagState(state.query(), chunks, null, null);\n};\n</code></pre>"},{"location":"tutorial/09-rag-pipeline/#augment-node","title":"Augment node","text":"<p>Assembles the system prompt by injecting retrieved context:</p> <pre><code>Node&lt;RagState&gt; augmentNode = (state, ctx) -&gt; {\n    String context = String.join(\"\\n---\\n\", state.retrievedChunks());\n    String systemPrompt = \"\"\"\n        Answer the question using only the context below.\n        Context:\n        %s\n        \"\"\".formatted(context);\n    return new RagState(state.query(), state.retrievedChunks(), systemPrompt, null);\n};\n</code></pre>"},{"location":"tutorial/09-rag-pipeline/#generate-node","title":"Generate node","text":"<p>Calls the LLM with the augmented prompt:</p> <pre><code>LlmClient llm = OpenAiLlmClient.builder()\n    .apiKey(System.getenv(\"OPENAI_API_KEY\"))\n    .model(\"gpt-4o-mini\")\n    .build();\n\nNode&lt;RagState&gt; generateNode = ChatNode.&lt;RagState&gt;builder()\n    .client(llm)\n    .requestBuilder(state -&gt; LlmRequest.builder()\n        .systemMessage(state.systemPrompt())\n        .message(ChatMessage.user(state.query()))\n        .model(\"gpt-4o-mini\")\n        .maxTokens(1024)\n        .build())\n    .responseFolder((state, response) -&gt;\n        new RagState(state.query(), state.retrievedChunks(), state.systemPrompt(), response.content()))\n    .build();\n</code></pre>"},{"location":"tutorial/09-rag-pipeline/#assemble-the-graph","title":"Assemble the graph","text":"<pre><code>Graph&lt;RagState&gt; ragGraph = Graph.&lt;RagState&gt;builder()\n    .node(\"retrieve\", retrieveNode, RetryPolicy.of(3, BackoffStrategy.exponential(200, 5_000)))\n    .node(\"augment\",  augmentNode)\n    .node(\"generate\", generateNode, RetryPolicy.of(2, BackoffStrategy.fixed(1_000)))\n    .edge(\"retrieve\", \"augment\")\n    .edge(\"augment\",  \"generate\")\n    .entry(\"retrieve\")\n    .terminal(\"generate\")\n    .build();\n</code></pre>"},{"location":"tutorial/09-rag-pipeline/#adding-a-reranker","title":"Adding a reranker","text":"<p>Insert a rerank node between retrieval and augmentation to improve chunk relevance:</p> <pre><code>Node&lt;RagState&gt; rerankNode = (state, ctx) -&gt; {\n    List&lt;String&gt; reranked = reranker.rerank(state.query(), state.retrievedChunks(), 3);\n    return state.withRetrievedChunks(reranked);\n};\n\n// Add .node(\"rerank\", rerankNode) and .edge(\"retrieve\", \"rerank\"), .edge(\"rerank\", \"augment\")\n</code></pre>"},{"location":"tutorial/09-rag-pipeline/#key-takeaways","title":"Key takeaways","text":"<ul> <li>Each RAG step (retrieve, augment, generate) is a separate node \u2014 each gets its own retry policy and trace step.</li> <li><code>ChatNode</code> handles the LLM call with automatic token reporting.</li> <li>The graph is observable end-to-end: every step's before/after state appears in <code>ExecutionTrace</code>.</li> <li>Swap in any vector store by changing the <code>retrieveNode</code> implementation \u2014 the rest of the graph is unchanged.</li> </ul>"},{"location":"tutorial/10-replay-and-diff/","title":"10 \u2014 Replay &amp; Diff","text":"<p>Replay lets you re-execute a saved trace from any step \u2014 essential for debugging, prompt iteration, and regression testing. Diff compares two traces structurally.</p>"},{"location":"tutorial/10-replay-and-diff/#recording-a-trace","title":"Recording a trace","text":"<p>Wire a <code>TraceRecorder</code> and <code>TraceStore</code> at graph build time:</p> <pre><code>InMemoryTraceStore&lt;RagState&gt; traceStore = new InMemoryTraceStore&lt;&gt;();\nTraceRecorder&lt;RagState&gt; recorder = new RecordingTraceRecorder&lt;&gt;(traceStore);\n\nGraph&lt;RagState&gt; graph = Graph.&lt;RagState&gt;builder()\n    // ... nodes and edges ...\n    .traceRecorder(recorder)\n    .build();\n\nExecutionResult&lt;RagState&gt; result = graph.run(RagState.of(\"What is the capital of France?\"));\nString executionId = result.executionId();\n</code></pre>"},{"location":"tutorial/10-replay-and-diff/#inspecting-the-trace","title":"Inspecting the trace","text":"<pre><code>ExecutionTrace&lt;RagState&gt; trace = traceStore.load(executionId).orElseThrow();\n\nfor (TraceStep&lt;RagState&gt; step : trace.steps()) {\n    System.out.printf(\"%-12s  attempts=%d  before=%s%n\",\n        step.nodeName(), step.attempts(), step.before());\n}\n</code></pre> <p>Each <code>TraceStep</code> records <code>nodeName</code>, <code>before</code> state, <code>after</code> state, <code>attempts</code> count, and per-step <code>usage</code> (prompt/completion tokens for LLM nodes).</p>"},{"location":"tutorial/10-replay-and-diff/#replaying-from-a-step","title":"Replaying from a step","text":"<p><code>ReplayRunner</code> re-executes from a chosen step index against the same (or a modified) graph:</p> <pre><code>Graph&lt;RagState&gt; improvedGraph = // ... graph with a better prompt ...\n\nReplayRunner&lt;RagState&gt; runner = ReplayRunner.of(trace, improvedGraph);\nExecutionResult&lt;RagState&gt; forked = runner.reRunFrom(1);  // re-run from step index 1\n\nSystem.out.println(forked.executionId());          // new UUID\nSystem.out.println(forked.forkedFromExecutionId()); // original UUID\nSystem.out.println(forked.forkedFromStepIndex());   // 1\n</code></pre> <p>Pass <code>stepIndex = -1</code> to replay from the entry node using the original seed state. The new <code>ExecutionTrace</code> carries <code>forkedFromExecutionId</code> and <code>forkedFromStepIndex</code> to record lineage.</p>"},{"location":"tutorial/10-replay-and-diff/#supplying-an-alternative-seed","title":"Supplying an alternative seed","text":"<p>Override the seed state for the replay by passing a second argument:</p> <pre><code>RagState alternativeSeed = trace.steps().get(1).before().withQuery(\"Different question?\");\nExecutionResult&lt;RagState&gt; forked = runner.reRunFrom(1, alternativeSeed);\n</code></pre>"},{"location":"tutorial/10-replay-and-diff/#comparing-two-traces","title":"Comparing two traces","text":"<p><code>TraceDiff.between(left, right)</code> walks two traces step-by-step and finds the longest common prefix:</p> <pre><code>ExecutionTrace&lt;RagState&gt; original = traceStore.load(executionId).orElseThrow();\nExecutionTrace&lt;RagState&gt; forked   = traceStore.load(forked.executionId()).orElseThrow();\n\nTraceDiff&lt;RagState&gt; diff = TraceDiff.between(original, forked);\n\nSystem.out.println(\"Diverged at step: \" + diff.divergenceIndex());\nSystem.out.println(\"Same final state: \" + diff.sameFinalState());\nSystem.out.println(\"Identical:        \" + diff.identical());\n</code></pre> <p><code>diff.leftRemainder()</code> and <code>diff.rightRemainder()</code> contain the steps after divergence in each trace.</p>"},{"location":"tutorial/10-replay-and-diff/#persistent-traces-with-jsonfiletracestore","title":"Persistent traces with JsonFileTraceStore","text":"<pre><code>TraceStore&lt;RagState&gt; fileStore = JsonFileTraceStore.of(\n    Path.of(\"/var/tracegraph/traces\"),\n    RagState.class\n);\n</code></pre> <p>Files are written atomically (<code>*.tmp</code> + <code>ATOMIC_MOVE</code>). Throwable round-trip is lossy \u2014 only class name and message are preserved.</p>"},{"location":"tutorial/10-replay-and-diff/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>RecordingTraceRecorder</code> + <code>InMemoryTraceStore</code> (or <code>JsonFileTraceStore</code>) captures full step-by-step history.</li> <li><code>ReplayRunner.of(trace, graph).reRunFrom(stepIndex)</code> re-executes from any step with a new executionId.</li> <li>The forked trace carries <code>forkedFromExecutionId</code> + <code>forkedFromStepIndex</code> for lineage tracking.</li> <li><code>TraceDiff.between(a, b)</code> finds the first divergence point and compares final state.</li> </ul>"},{"location":"tutorial/11-hitl-interrupts/","title":"11 \u2014 HITL Interrupts","text":"<p>Human-in-the-Loop (HITL) pauses let an operator inspect or approve a graph's state before execution continues. TraceGraph implements this as a first-class interrupt mechanism \u2014 no polling, no sleep loops.</p>"},{"location":"tutorial/11-hitl-interrupts/#interruptbefore-and-interruptafter","title":"interruptBefore and interruptAfter","text":"<p>Declare interrupt points at graph build time:</p> <pre><code>Graph&lt;ApprovalState&gt; graph = Graph.&lt;ApprovalState&gt;builder()\n    .node(\"draft\",   draftNode)\n    .node(\"review\",  reviewNode)\n    .node(\"publish\", publishNode)\n    .edge(\"draft\",   \"review\")\n    .edge(\"review\",  \"publish\")\n    .entry(\"draft\")\n    .terminal(\"publish\")\n    .checkpointStore(checkpointStore)\n    .interruptBefore(\"publish\")   // pause before publishing; operator must approve\n    .build();\n</code></pre> <p><code>interruptBefore(\"publish\")</code> pauses the executor just before <code>publish</code> would run and writes a checkpoint. <code>interruptAfter(\"review\")</code> runs <code>review</code> to completion, writes the checkpoint, then pauses.</p>"},{"location":"tutorial/11-hitl-interrupts/#running-to-an-interrupt","title":"Running to an interrupt","text":"<pre><code>ExecutionResult&lt;ApprovalState&gt; result = graph.run(ApprovalState.of(\"Draft content...\"));\nSystem.out.println(result.status());      // INTERRUPTED\nSystem.out.println(result.executionId()); // save this to resume later\n</code></pre> <p>When the executor hits an <code>interruptBefore</code> point it sets <code>Status.INTERRUPTED</code> and returns. The state at that moment is saved in the checkpoint store.</p>"},{"location":"tutorial/11-hitl-interrupts/#inspecting-state-before-resuming","title":"Inspecting state before resuming","text":"<p>Load the checkpoint (or trace) to see the pending state:</p> <pre><code>ApprovalState pending = checkpointStore.load(executionId)\n    .map(Checkpoint::state)\n    .orElseThrow();\n\nSystem.out.println(pending.draft()); // operator reviews this\n</code></pre>"},{"location":"tutorial/11-hitl-interrupts/#resuming-after-approval","title":"Resuming after approval","text":"<pre><code>// Operator approved \u2014 resume\nExecutionResult&lt;ApprovalState&gt; completed = graph.resume(executionId);\nSystem.out.println(completed.status()); // COMPLETED\n</code></pre> <p><code>graph.resume(id)</code> continues from the checkpoint. The <code>publish</code> node runs normally.</p>"},{"location":"tutorial/11-hitl-interrupts/#spring-boot-rest-endpoint","title":"Spring Boot REST endpoint","text":"<p>When using the Spring Boot starter, the interrupt/resume flow has REST endpoints out of the box:</p> <pre><code># Check whether a run is interrupted\nGET /tracegraph/traces/{id}        \u2192 { \"status\": \"INTERRUPTED\", ... }\n\n# Resume\nPOST /tracegraph/traces/{id}/resume\n\u2192 200 { \"status\": \"COMPLETED\" }   (or 404 if unknown, 409 if not INTERRUPTED)\n</code></pre>"},{"location":"tutorial/11-hitl-interrupts/#handling-modifications-before-resume","title":"Handling modifications before resume","text":"<p>If the operator wants to modify state before continuing, load, modify, and re-save the checkpoint before calling <code>resume</code>:</p> <pre><code>Checkpoint&lt;ApprovalState&gt; cp = checkpointStore.load(executionId).orElseThrow();\nApprovalState approved = cp.state().withApproverNote(\"LGTM \u2014 proceed.\");\ncheckpointStore.save(cp.withState(approved));\n\ngraph.resume(executionId);\n</code></pre>"},{"location":"tutorial/11-hitl-interrupts/#key-takeaways","title":"Key takeaways","text":"<ul> <li><code>interruptBefore(name)</code> pauses before the named node; <code>interruptAfter(name)</code> pauses after it.</li> <li>Interrupted runs return <code>Status.INTERRUPTED</code> and write a checkpoint \u2014 they do not throw.</li> <li><code>graph.resume(id)</code> picks up from the checkpoint; the interrupted node runs normally on resume.</li> <li>Per-branch interrupts inside <code>parallel(...)</code> are not supported.</li> <li>The Spring Boot starter exposes <code>POST /tracegraph/traces/{id}/resume</code> for REST-based approval flows.</li> </ul>"}]}