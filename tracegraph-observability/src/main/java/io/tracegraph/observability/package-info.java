/**
 * OpenTelemetry-backed observability for TraceGraph: a NodeListener that emits a
 * span per node with retries surfaced as span events. Plug in via Graph.Builder.listener(...).
 */
package io.tracegraph.observability;
